1. 필터링 순위: -a, -g, -u (이들은 중첩되어 적용됩니다. 즉, -u -g이면 '정의되지 않은 전역 심볼'만 나옵니다.)

2. 정렬 순위: -n (주소 정렬) > 기본 정렬(알파벳) 순으로 검토된 후, **-r**이 최종 방향을 결정합니다.

3. 형식 순위: -P (출력 포맷 고정)

a:
type N, a 을 출력에 포함 시킵니다.
g:
type 이 대문자 'A', 'B', 'D', 'R', 'T', 'W', 'w'
u:
type == U || type == w 만 출력 합니다.


n:
quick sort 를 사용한다.
shdr_64->st_value 를 기준으로 오름차순
n이 없다면 
const char  *SymName 기준으로 오름 차순

r:
quick sort 를 사용한다.
n 이 존재한다면
shdr_64->st_value 를 기준으로 내림 차순
n 이 존재하지 않는다면
const char  *SymName 기준으로 내림 차순
